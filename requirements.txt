# LLM providers; only need the SDK for whichever provider you actually use
anthropic>=0.40.0
openai>=1.50.0

# Local embeddings for semantic similarity scoring
sentence-transformers>=3.0.0
numpy>=1.26.0

# Resume file parsing
pdfplumber>=0.11.0
python-docx>=1.1.0

# Config + testing
python-dotenv>=1.0.0
pytest>=8.0.0
